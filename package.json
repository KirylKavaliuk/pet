{
  "private": true,
  "name": "pet",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:ui": "lerna run build --scope=@kirxray43/ui",
    "build:api": "lerna run build --scope=@kirxray43/api",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^5.1.4"
  }
}
